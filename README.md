Here is my neovim config, use at your own risk!

requirements:
```
npm
ripgrep
git
fd
unzip
tree-sitter-cli (npm install -g tree-sitter-cli)
xregexp (npm install -g xregexp)
```
[ripgrep](https://github.com/BurntSushi/ripgrep)
